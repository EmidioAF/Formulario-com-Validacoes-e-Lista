body { font-family: Arial, sans-serif; margin: 32px; }
form { max-width: 400px; margin-bottom: 24px; }
label { display: block; margin-top: 16px; }
input[type="text"], input[type="email"], input[type="password"], input[type="date"] { width: 100%; padding: 8px; margin-top: 4px; }
.erro { color: red; font-size: 0.95em; }
button[disabled] { background: #ccc; }
ul { padding-left: 0; list-style: none; }
li { background: #f9f9f9; margin-bottom: 8px; padding: 8px; border-radius: 4px; }
